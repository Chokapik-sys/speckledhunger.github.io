  var num1 = parseint(prompt("Pick a number"))
  var num2 = parseint(prompt("Pick another number"))
  var sign = parseint(prompt("Choose to multiply (*), add (+), divide (/) or minus (-)"))
  var product = num1 + sign + num2
  alert (num1 + " " + sign + " " + num2 + " " + "=" + " " + product)
